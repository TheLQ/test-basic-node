pipeline {
	agent any

	environment {
		WIN_LINUX  = "brailleblaster-daily-linux.zip"
        OTHER_var = "asdf"
    }

    stages {
        stage("first") {
            steps {
                sh "env"
            }
        }
    }
}
